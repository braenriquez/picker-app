{
  "name": "picker-inventory-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Centralized inventory backend for the Gruppo Bravo picker PWA. Public /api via Tailscale Funnel, tailnet-only /admin via Tailscale Serve.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "init-db": "node scripts/init-db.js",
    "ingest": "node scripts/ingest-cli.js",
    "newkey": "node scripts/newkey-cli.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/multipart": "^8.3.0",
    "@fastify/static": "^7.0.4",
    "better-sqlite3": "^11.8.0",
    "dotenv": "^16.4.7",
    "fastify": "^4.29.0",
    "jose": "^5.9.6",
    "libsodium-wrappers": "^0.7.15",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  }
}
